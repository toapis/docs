---
title: "Qwen3 Max Chat"
sidebarTitle: "qwen3-max"
api: "POST https://toapis.com/v1/chat/completions"
description: "Alibaba's flagship reasoning model with deep thinking mode, excelling at complex reasoning, code generation, and long-context tasks"
---

- OpenAI-compatible Chat Completions API
- Select the `qwen3-max` model via the `model` parameter
- Supports streaming output (SSE)
- Supports multi-turn conversation and system prompts
- Extended Thinking mode for complex multi-step reasoning
- Text-only input; vision input (images) is not supported


## Authorizations

<ParamField header="Authorization" type="string" required>
  All endpoints require Bearer Token authentication

  Get your API Key from the [API Key Management Page](https://toapis.com/console/token)

  Add to request header:
  ```
  Authorization: Bearer YOUR_API_KEY
  ```
</ParamField>

## Body

<ParamField body="model" type="string" default="qwen3-max" required>
  モデル name

  Example: `"qwen3-max"`
</ParamField>

<ParamField body="messages" type="object[]" required>
  List of messages comprising the conversation, ordered chronologically

  <Expandable title="messages[n]">
    <ParamField body="role" type="string" required>
      Role of the message author: `system`, `user`, or `assistant`
    </ParamField>
    <ParamField body="content" type="string" required>
      The content of the message
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="stream" type="boolean" default={false}>
  Whether to stream the response (Server-Sent Events)

  - `true`: Stream tokens incrementally as they are generated
  - `false`: Return the complete response at once
</ParamField>

<ParamField body="max_tokens" type="integer">
  Maximum number of tokens to generate

  Uses the model's default maximum when not specified
</ParamField>

<ParamField body="temperature" type="number" default={1}>
  Sampling temperature controlling output randomness

  - Range: `0` ~ `2`
  - Lower values produce more deterministic output; higher values produce more random output
</ParamField>

<ParamField body="top_p" type="number" default={1}>
  Nucleus sampling probability threshold

  Range: `0` ~ `1`. Avoid modifying both `temperature` and `top_p` simultaneously
</ParamField>

<ParamField body="stop" type="string | string[]">
  Stop sequences — generation halts when any of these strings are encountered

  Up to 4 stop sequences
</ParamField>

## レスポンス

<ResponseField name="id" type="string">
  Unique identifier for the completion request
</ResponseField>

<ResponseField name="object" type="string">
  Object type, always `chat.completion`
</ResponseField>

<ResponseField name="created" type="integer">
  Unix timestamp of when the request was created
</ResponseField>

<ResponseField name="model" type="string">
  The model that was actually used
</ResponseField>

<ResponseField name="choices" type="object[]">
  List of generated completions

  - `choices[].message.role`: Role of the response, always `assistant`
  - `choices[].message.content`: Generated text content
  - `choices[].finish_reason`: Stop reason — `stop` / `length` / `content_filter`
  - `choices[].index`: Index of this choice
</ResponseField>

<ResponseField name="usage" type="object">
  Token usage statistics for the request

  - `usage.prompt_tokens`: Number of input tokens
  - `usage.completion_tokens`: Number of output tokens
  - `usage.total_tokens`: Total tokens consumed
</ResponseField>

<RequestExample>
```bash cURL
curl --request POST \
  --url https://toapis.com/v1/chat/completions \
  --header 'Authorization: Bearer <token>' \
  --header 'Content-Type: application/json' \
  --data '{
    "model": "qwen3-max",
    "messages": [
      {
        "role": "user",
        "content": "Analyze the impact of quantum computing on modern cryptography"
      }
    ]
  }'
```

```bash cURL (Streaming)
curl --request POST \
  --url https://toapis.com/v1/chat/completions \
  --header 'Authorization: Bearer <token>' \
  --header 'Content-Type: application/json' \
  --data '{
    "model": "qwen3-max",
    "messages": [
      {
        "role": "system",
        "content": "You are a senior software architect"
      },
      {
        "role": "user",
        "content": "Design a high-concurrency order processing system"
      }
    ],
    "stream": true
  }'
```

```python Python (openai SDK)
from openai import OpenAI

client = OpenAI(
    api_key="your-ToAPIs-key",
    base_url="https://toapis.com/v1"
)

response = client.chat.completions.create(
    model="qwen3-max",
    messages=[
        {"role": "user", "content": "Analyze the impact of quantum computing on modern cryptography"}
    ]
)

print(response.choices[0].message.content)
```

```python Python (Streaming)
from openai import OpenAI

client = OpenAI(
    api_key="your-ToAPIs-key",
    base_url="https://toapis.com/v1"
)

stream = client.chat.completions.create(
    model="qwen3-max",
    messages=[
        {"role": "system", "content": "You are a senior software architect"},
        {"role": "user", "content": "Design a high-concurrency order processing system"}
    ],
    stream=True
)

for chunk in stream:
    if chunk.choices[0].delta.content:
        print(chunk.choices[0].delta.content, end="", flush=True)
```

```javascript JavaScript (openai SDK)
import OpenAI from "openai";

const client = new OpenAI({
  apiKey: "your-ToAPIs-key",
  baseURL: "https://toapis.com/v1",
});

const response = await client.chat.completions.create({
  model: "qwen3-max",
  messages: [{ role: "user", content: "Analyze the impact of quantum computing on modern cryptography" }],
});

console.log(response.choices[0].message.content);
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": "chatcmpl-abc123",
  "object": "chat.completion",
  "created": 1703884800,
  "model": "qwen3-max",
  "choices": [
    {
      "index": 0,
      "message": {
        "role": "assistant",
        "content": "Quantum computing poses a fundamental threat to modern cryptography. Asymmetric encryption algorithms like RSA and ECC rely on the computational hardness of integer factorization and discrete logarithm problems. Shor's algorithm, running on a sufficiently large quantum computer, can solve these problems in polynomial time, rendering current public-key infrastructure vulnerable."
      },
      "finish_reason": "stop"
    }
  ],
  "usage": {
    "prompt_tokens": 14,
    "completion_tokens": 72,
    "total_tokens": 86
  }
}
```

```json 400
{
  "error": {
    "code": "invalid_request_error",
    "message": "The 'messages' field is required.",
    "param": "messages",
    "type": "invalid_request_error"
  }
}
```

```json 401
{
  "error": {
    "code": "unauthorized",
    "message": "Invalid API key",
    "type": "authentication_error"
  }
}
```
</ResponseExample>
