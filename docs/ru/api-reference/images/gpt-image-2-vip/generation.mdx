---
title: "GPT-Image-2-VIP Генерация изображений"
sidebarTitle: "gpt-image-2-vip"
api: "POST https://toapis.com/v1/images/generations"
description: "Генерация изображений с помощью gpt-image-2-vip. Параметры совпадают с gpt-image-2, но поддерживаются все стандартные соотношения сторон."
---

- Унифицированный API генерации изображений
- Выбор модели `gpt-image-2-vip` через параметр `model`
- Параметры совпадают с `gpt-image-2`
- Поддержка text-to-image, single-reference и multi-reference generation
- Асинхронное управление задачами с получением результата по task ID

<Warning>
**Важное изменение**: Для повышения производительности и контроля затрат base64 больше не поддерживается в `image_urls` и `reference_images`. Сначала используйте [API загрузки изображений](../../uploads/images), затем передайте полученный URL.
</Warning>

## Авторизация

<ParamField header="Authorization" type="string" required>
  Все API требуют аутентификации Bearer Token
  
  Получите API Key на [странице управления API ключами](https://toapis.com/console/token)
  
  Добавьте в заголовок:
  ```
  Authorization: Bearer YOUR_API_KEY
  ```
</ParamField>

## Тело запроса

<ParamField body="model" type="string" default="gpt-image-2-vip" required>
  Название модели генерации изображений
  
  Пример: `"gpt-image-2-vip"`
</ParamField>

<ParamField body="prompt" type="string" required>
  Текстовый промпт для генерации изображения
  
  Максимум 32 000 символов для GPT image models
</ParamField>

<ParamField body="size" type="string" default="1:1">
  Соотношение сторон выходного изображения
  
  Сейчас поддерживаются следующие соотношения:
  
  `1:1`, `3:2`, `2:3`, `4:3`, `3:4`, `5:4`, `4:5`, `16:9`, `9:16`, `2:1`, `1:2`, `21:9`, `9:21`
  
  `auto` временно не поддерживается.
</ParamField>

<ParamField body="resolution" type="string" default="1k">
  Уровень разрешения изображения
  
  Поддерживаемые значения: `1k`, `2k`, `4k`
</ParamField>

<ParamField body="quality" type="string" default="medium">
  Качество изображения

  - `low` — быстро и экономично, для черновиков/превью
  - `medium` — баланс скорости и качества
  - `high` — максимальная детализация, подходит для финальных изображений
</ParamField>

<ParamField body="n" type="integer" default={1}>
  Количество изображений
  
  По умолчанию: 1
</ParamField>

<ParamField body="response_format" type="string" default="url">
  Формат ответа
  
  API возвращает URL изображения, рекомендуемое значение: `url`
</ParamField>

<ParamField body="reference_images" type="string[]">
  URL эталонных изображений для image-to-image
  
  **⚠️ Только URL (base64 больше не поддерживается)**
  
  - Публично доступные URL (`http://` или `https://`)
  - Можно использовать [API загрузки изображений](../../uploads/images) для загрузки локальных файлов
  - Поддерживаются одиночные и множественные reference images
</ParamField>

<ParamField body="image_urls" type="string[]">
  Поле обратной совместимости
  
  В ToAPIs автоматически нормализуется в `reference_images`
</ParamField>

<ParamField body="callback_url" type="string">
  Стандартный callback завершения ToAPIs. Настройте HTTPS URL и secret Token; разрешено только same-origin переопределение. См. [Webhook](/docs/ru/api-reference/webhooks/task-webhooks).
</ParamField>

## Ответ

<ResponseField name="id" type="string">
  Уникальный идентификатор задачи
</ResponseField>

<ResponseField name="object" type="string">
  Тип объекта, всегда `generation.task`
</ResponseField>

<ResponseField name="model" type="string">
  Используемая модель
</ResponseField>

<ResponseField name="status" type="string">
  Статус задачи
  - `queued`
  - `in_progress`
  - `completed`
  - `failed`
</ResponseField>

<ResponseField name="progress" type="integer">
  Прогресс задачи (0-100)
</ResponseField>

<ResponseField name="created_at" type="integer">
  Время создания задачи (Unix timestamp)
</ResponseField>

<RequestExample>
```bash cURL
curl --request POST \
  --url https://toapis.com/v1/images/generations \
  --header 'Authorization: Bearer <token>' \
  --header 'Content-Type: application/json' \
  --data '{
    "model": "gpt-image-2-vip",
    "prompt": "Сгенерируй широкоформатный кинематографичный постер ночного города будущего с неоном",
    "n": 1,
    "size": "16:9",
    "resolution": "2k",
    "quality": "high",
    "response_format": "url"
  }'
```

```bash cURL (Image-to-Image)
curl --request POST \
  --url https://toapis.com/v1/images/generations \
  --header 'Authorization: Bearer <token>' \
  --header 'Content-Type: application/json' \
  --data '{
    "model": "gpt-image-2-vip",
    "prompt": "Сохрани структуру сцены, но сделай стиль киберпанк с усиленным светом и деталями",
    "n": 1,
    "size": "9:16",
    "resolution": "4k",
    "quality": "medium",
    "image_urls": [
      "https://example.com/source.png",
      "https://example.com/source.png"
    ],
    "response_format": "url"
  }'
```

```javascript JavaScript
const response = await fetch('https://toapis.com/v1/images/generations', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer your-ToAPIs-key',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    model: 'gpt-image-2-vip',
    prompt: 'Сгенерируй широкоформатный кинематографичный постер ночного города будущего с неоном',
    n: 1,
    size: '16:9',
    resolution: '2k',
    quality: 'low',
    response_format: 'url'
  })
});

const task = await response.json();
console.log(task.id, task.status);
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": "task_img_abc123def456",
  "object": "generation.task",
  "model": "gpt-image-2-vip",
  "status": "queued",
  "progress": 0,
  "created_at": 1703884800,
  "metadata": {}
}
```
</ResponseExample>
