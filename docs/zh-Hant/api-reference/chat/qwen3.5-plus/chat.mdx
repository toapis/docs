---
title: "Qwen3.5 Plus 對話"
sidebarTitle: "qwen3.5-plus"
api: "POST https://toapis.com/v1/chat/completions"
description: "阿里千問高性價比旗艦模型，綜合能力均衡，適合企業級多場景部署"
---

- 兼容 OpenAI Chat Completions API 格式
- 通過 `model` 參數指定 `qwen3.5-plus` 模型
- 支持流式輸出（SSE）
- 支持多輪對話、系統提示詞
- 超長上下文窗口，最高支持 256K token 輸入
- 支持視覺輸入（圖片理解），可傳入圖片 URL 或 Base64 數據


## Authorizations

<ParamField header="Authorization" type="string" required>
  所有接口均需要使用 Bearer Token 進行認證

  獲取 API Key：訪問 [API Key 管理頁面](https://toapis.com/console/token) 獲取您的 API Key

  使用時在請求頭中添加：
  ```
  Authorization: Bearer YOUR_API_KEY
  ```
</ParamField>

## Body

<ParamField body="model" type="string" default="qwen3.5-plus" required>
  模型名稱

  示例：`"qwen3.5-plus"`
</ParamField>

<ParamField body="messages" type="object[]" required>
  對話消息列表，按時間順序排列

  <Expandable title="messages[n]">
    <ParamField body="role" type="string" required>
      消息角色，可選值 `system`、`user`、`assistant`
    </ParamField>
    <ParamField body="content" type="string | array" required>
      消息內容，字符串或內容塊數組。支持視覺輸入時，傳入數組格式，包含 `text` 和 `image_url` 類型的內容塊
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="stream" type="boolean" default={false}>
  是否啓用流式輸出（Server-Sent Events）

  - `true`：逐 token 流式返回
  - `false`：等待完整響應後一次性返回
</ParamField>

<ParamField body="max_tokens" type="integer">
  生成內容的最大 token 數量

  不設置時使用模型默認上限
</ParamField>

<ParamField body="temperature" type="number" default={1}>
  採樣溫度，控制輸出隨機性

  - 範圍：`0` ~ `2`
  - 值越低輸出越穩定，值越高輸出越隨機
</ParamField>

<ParamField body="top_p" type="number" default={1}>
  核採樣概率閾值

  範圍：`0` ~ `1`，建議不要同時修改 `temperature` 和 `top_p`
</ParamField>

<ParamField body="stop" type="string | string[]">
  停止序列，遇到指定字符串時停止生成

  最多 4 個停止序列
</ParamField>

## Response

<ResponseField name="id" type="string">
  本次請求的唯一標識符
</ResponseField>

<ResponseField name="object" type="string">
  對象類型，固定爲 `chat.completion`
</ResponseField>

<ResponseField name="created" type="integer">
  請求創建時間（Unix 時間戳）
</ResponseField>

<ResponseField name="model" type="string">
  實際使用的模型名稱
</ResponseField>

<ResponseField name="choices" type="object[]">
  生成結果列表

  - `choices[].message.role`：消息角色，固定爲 `assistant`
  - `choices[].message.content`：生成的文本內容
  - `choices[].finish_reason`：結束原因，`stop` / `length` / `content_filter`
  - `choices[].index`：結果索引
</ResponseField>

<ResponseField name="usage" type="object">
  本次請求的 token 消耗統計

  - `usage.prompt_tokens`：輸入 token 數
  - `usage.completion_tokens`：輸出 token 數
  - `usage.total_tokens`：總 token 數
</ResponseField>

<RequestExample>
```bash cURL
curl --request POST \
  --url https://toapis.com/v1/chat/completions \
  --header 'Authorization: Bearer <token>' \
  --header 'Content-Type: application/json' \
  --data '{
    "model": "qwen3.5-plus",
    "messages": [
      {
        "role": "user",
        "content": "幫我寫一份項目需求說明書的大綱"
      }
    ]
  }'
```

```bash cURL（視覺輸入）
curl --request POST \
  --url https://toapis.com/v1/chat/completions \
  --header 'Authorization: Bearer <token>' \
  --header 'Content-Type: application/json' \
  --data '{
    "model": "qwen3.5-plus",
    "messages": [
      {
        "role": "user",
        "content": [
          {
            "type": "image_url",
            "image_url": { "url": "https://www.gstatic.com/webp/gallery/1.jpg" }
          },
          {
            "type": "text",
            "text": "描述這張圖片"
          }
        ]
      }
    ]
  }'
```

```bash cURL（流式輸出）
curl --request POST \
  --url https://toapis.com/v1/chat/completions \
  --header 'Authorization: Bearer <token>' \
  --header 'Content-Type: application/json' \
  --data '{
    "model": "qwen3.5-plus",
    "messages": [
      {
        "role": "system",
        "content": "你是一個資深產品經理"
      },
      {
        "role": "user",
        "content": "幫我寫一份項目需求說明書的大綱"
      }
    ],
    "stream": true
  }'
```

```python Python (openai SDK)
from openai import OpenAI

client = OpenAI(
    api_key="your-ToAPIs-key",
    base_url="https://toapis.com/v1"
)

response = client.chat.completions.create(
    model="qwen3.5-plus",
    messages=[
        {"role": "user", "content": "幫我寫一份項目需求說明書的大綱"}
    ]
)

print(response.choices[0].message.content)
```

```python Python（流式輸出）
from openai import OpenAI

client = OpenAI(
    api_key="your-ToAPIs-key",
    base_url="https://toapis.com/v1"
)

stream = client.chat.completions.create(
    model="qwen3.5-plus",
    messages=[
        {"role": "system", "content": "你是一個資深產品經理"},
        {"role": "user", "content": "幫我寫一份項目需求說明書的大綱"}
    ],
    stream=True
)

for chunk in stream:
    if chunk.choices[0].delta.content:
        print(chunk.choices[0].delta.content, end="", flush=True)
```

```javascript JavaScript (openai SDK)
import OpenAI from "openai";

const client = new OpenAI({
  apiKey: "your-ToAPIs-key",
  baseURL: "https://toapis.com/v1",
});

const response = await client.chat.completions.create({
  model: "qwen3.5-plus",
  messages: [{ role: "user", content: "幫我寫一份項目需求說明書的大綱" }],
});

console.log(response.choices[0].message.content);
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": "chatcmpl-abc456",
  "object": "chat.completion",
  "created": 1703884800,
  "model": "qwen3.5-plus",
  "choices": [
    {
      "index": 0,
      "message": {
        "role": "assistant",
        "content": "以下是項目需求說明書的大綱框架：\n\n1. 項目概述\n   - 項目背景與目標\n   - 範圍與邊界\n\n2. 業務需求\n   - 核心功能列表\n   - 用戶角色與權限\n\n3. 非功能需求\n   - 性能指標\n   - 安全要求\n\n4. 接口與集成\n5. 驗收標準"
      },
      "finish_reason": "stop"
    }
  ],
  "usage": {
    "prompt_tokens": 14,
    "completion_tokens": 98,
    "total_tokens": 112
  }
}
```

```json 400
{
  "error": {
    "code": "invalid_request_error",
    "message": "The 'messages' field is required.",
    "param": "messages",
    "type": "invalid_request_error"
  }
}
```

```json 401
{
  "error": {
    "code": "unauthorized",
    "message": "Invalid API key",
    "type": "authentication_error"
  }
}
```
</ResponseExample>
