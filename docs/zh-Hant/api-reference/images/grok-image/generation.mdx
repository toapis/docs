---
title: "Grok 圖像生成"
sidebarTitle: "Grok Image"
api: "POST https://toapis.com/v1/images/generations"
description: "使用 Grok 模型生成圖像，支持文本到圖像和圖生圖"
---

- 統一的圖像生成 API 接口
- 通過 model 參數選擇 `grok-4-1-image` 或 `grok-4-2-image` 模型
- 支持文本到圖像和圖生圖
- 異步任務管理，通過任務 ID 查詢結果

<Warning>
**重要變更**：爲了更好的性能和成本控制，我們不再支持在 `image_urls` 中直接傳入 base64 圖片數據。請先使用 [上傳圖片接口](../../uploads/images) 上傳圖片，獲取 URL 後再調用本接口。
</Warning>

## Authorizations

<ParamField header="Authorization" type="string" required>
  所有接口均需要使用 Bearer Token 進行認證

  獲取 API Key：訪問 [API Key 管理頁面](https://toapis.com/console/token) 獲取您的 API Key

  使用時在請求頭中添加：
  ```
  Authorization: Bearer YOUR_API_KEY
  ```
</ParamField>

## Body

<ParamField body="model" type="string" required>
  圖像生成模型名稱

  支持的模型：
  - `grok-4-1-image` - Grok 4.1 圖像生成模型
  - `grok-4-2-image` - Grok 4.2 圖像生成模型

  示例：`"grok-4-1-image"`
</ParamField>

<ParamField body="prompt" type="string" required>
  圖像生成的文本描述

  最長 1000 個字符
</ParamField>

<ParamField body="size" type="string" default="1:1">
  圖像生成的尺寸比例

  支持的格式：
  - `1:1` - 正方形（默認）
  - `2:3` - 豎版
  - `3:2` - 橫版
  - `16:9` - 寬屏橫版
  - `9:16` - 寬屏豎版
</ParamField>

<ParamField body="n" type="integer" default={1}>
  生成圖像的數量

  支持 1、2、4，會根據生成數量進行預扣費

  默認：1

  **⚠️ 注意：** 必須輸入純數字（如 `1`），不要加引號，否則會報錯
</ParamField>

<ParamField body="image_urls" type="string[]">
  參考圖像 URL 列表，用於圖生圖

  **⚠️ 僅支持 URL 格式（不再支持 base64）**

  - 公開可訪問的圖片 URL（http:// 或 https://）
  - 可使用 [上傳圖片接口](../../uploads/images) 上傳本地圖片獲取 URL

  **限制：**
  - 最多 5 張圖片
  - 單張圖片不超過 10MB
  - 支持格式：jpeg、jpg、png、webp
</ParamField>

## Response

<ResponseField name="id" type="string">
  任務唯一標識符，用於查詢任務狀態
</ResponseField>

<ResponseField name="object" type="string">
  對象類型，固定爲 `generation.task`
</ResponseField>

<ResponseField name="model" type="string">
  使用的模型名稱
</ResponseField>

<ResponseField name="status" type="string">
  任務狀態
  - `queued` - 排隊等待處理
  - `in_progress` - 處理中
  - `completed` - 成功完成
  - `failed` - 失敗
</ResponseField>

<ResponseField name="progress" type="integer">
  任務進度百分比（0-100）
</ResponseField>

<ResponseField name="created_at" type="integer">
  任務創建時間戳（Unix 時間戳）
</ResponseField>

<ResponseField name="metadata" type="object">
  任務元數據
</ResponseField>

<RequestExample>
```bash cURL
curl --request POST \
  --url https://toapis.com/v1/images/generations \
  --header 'Authorization: Bearer <token>' \
  --header 'Content-Type: application/json' \
  --data '{
    "model": "grok-4-1-image",
    "prompt": "賽博朋克風格的未來城市夜景",
    "size": "16:9",
    "n": 1
  }'
```

```bash cURL (圖生圖)
curl --request POST \
  --url https://toapis.com/v1/images/generations \
  --header 'Authorization: Bearer <token>' \
  --header 'Content-Type: application/json' \
  --data '{
    "model": "grok-4-1-image",
    "prompt": "將這張圖片轉換爲水彩畫風格",
    "size": "1:1",
    "n": 1,
    "image_urls": [
      "https://example.com/photo.jpg"
    ]
  }'
```

```python Python
import requests

response = requests.post(
    "https://toapis.com/v1/images/generations",
    headers={
        "Authorization": "Bearer your-ToAPIs-key",
        "Content-Type": "application/json"
    },
    json={
        "model": "grok-4-1-image",
        "prompt": "賽博朋克風格的未來城市夜景",
        "size": "16:9",
        "n": 1
    }
)

task = response.json()
print(f"任務 ID: {task['id']}")
print(f"狀態: {task['status']}")
```

```javascript JavaScript
const response = await fetch('https://toapis.com/v1/images/generations', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer your-ToAPIs-key',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    model: 'grok-4-1-image',
    prompt: '賽博朋克風格的未來城市夜景',
    size: '16:9',
    n: 1
  })
});

const task = await response.json();
console.log(`任務 ID: ${task.id}`);
console.log(`狀態: ${task.status}`);
```
</RequestExample>

<ResponseExample>
```json 200
{
  "id": "img_abc123def456",
  "object": "generation.task",
  "model": "grok-4-1-image",
  "status": "queued",
  "progress": 0,
  "created_at": 1768380224,
  "metadata": {}
}
```

```json 400
{
  "error": {
    "code": "invalid_request",
    "message": "The 'prompt' field is required.",
    "param": "prompt",
    "type": "invalid_request_error"
  }
}
```

```json 401
{
  "error": {
    "code": "unauthorized",
    "message": "Invalid API key",
    "type": "authentication_error"
  }
}
```
</ResponseExample>
